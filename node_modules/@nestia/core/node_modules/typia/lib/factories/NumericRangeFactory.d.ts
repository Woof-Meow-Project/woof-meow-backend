import ts from "typescript";
import { ProtobufAtomic } from "../typings/ProtobufAtomic";
export declare namespace NumericRangeFactory {
    const number: (context: ts.TransformationContext) => (type: ProtobufAtomic.Numeric) => (input: ts.Expression) => ts.Expression;
    const bigint: (context: ts.TransformationContext) => (type: ProtobufAtomic.BigNumeric) => (input: ts.Expression) => ts.Expression;
}
